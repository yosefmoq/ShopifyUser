<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.app.shopifyuser">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the "MyLocation" functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ShopifyUser">
        <activity android:name=".user.ReportProblemsActivity"></activity>
        <activity android:name=".driver.DriverProfileActivity" />
        <activity android:name=".user.OrdersActivity" />
        <activity android:name=".driver.DeliveriesReceiptsActivity" />
        <activity android:name=".user.CartInfoActivity" />
        <activity android:name=".user.ActiveOrdersAcitivity" />
        <activity android:name=".driver.DriverRegisterationActivity" />
        <activity android:name=".user.MenuActivity" />
        <activity android:name=".driver.DriverActivity" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".user.MapsActivity"
            android:label="@string/title_activity_maps" />
        <activity android:name=".user.DetailsActivity" />
        <activity android:name=".user.CartActivity" />
        <activity android:name=".user.OrderActivity" />
        <activity android:name=".user.ResturantActivity" />
        <activity android:name=".user.DeleveryActivity" />
        <activity android:name=".user.UserActivity" />
        <activity android:name=".user.SignupActivity" />
        <activity android:name=".user.LoginActivity" />
        <activity android:name=".user.StartActivity" />
        <activity android:name=".user.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".user.MainActivity">

            <!-- <intent-filter> -->
            <!-- <action android:name="android.intent.action.MAIN" /> -->


            <!-- <category android:name="android.intent.category.LAUNCHER" /> -->
            <!-- </intent-filter> -->
        </activity>
    </application>

</manifest>